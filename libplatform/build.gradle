apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    compileOnly files('ext-libs/android_q.jar')
}

sourceCompatibility = "8"
targetCompatibility = "8"
